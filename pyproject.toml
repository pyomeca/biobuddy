[build-system]
requires = [
    "setuptools>=61", 
    "wheel",
]
build-backend = "setuptools.build_meta"

[project]
name = "biobuddy"
description = "A generic interface to generate a virtual buddy"
dynamic = ["version"]
authors = [
    {name = "EveCharbie", email = "eve.charbie@gmail.com"},
    {name = "Pariterre", email = "pariterre@hotmail.com"},
]
readme = "README.md"
license = { text = "MIT" }
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python"
]
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "ezc3d",
    "lxml",
    "scipy", 
    "pandas",
    "graphviz",
    "matplotlib",
    "yeadon",
]

[project.optional-dependencies]
gui = [
    "PySide6",
]
test = [
    "pytest",
    "black",
    "opensim",
    "biorbd",
    "deepdiff",
    "pyorerun",
    "bioviz", 
    "plotly",
]

[project.urls]
Documentation = "https://github.com/pyomeca/biobuddy/tree/main#readme"
Source = "https://github.com/pyomeca/biobuddy"
Tracker = "https://github.com/pyomeca/biobuddy/issues"

[project.scripts]
biobuddy-model-editor = "biobuddy.gui.model_editor_cli:main"
biobuddy-build-c3d-model = "biobuddy.gui.build_c3d_model:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["biobuddy*"]

[tool.setuptools.package-data]
biobuddy = ["gui/*.json"]

[tool.setuptools.dynamic]
version = { attr = "biobuddy.version.__version__" }

[tool.pytest.ini_options]
pythonpath = "."
testpaths = "tests"
